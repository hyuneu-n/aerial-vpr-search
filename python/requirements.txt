
# Python Dependencies for Faiss-DINO Benchmark
# Install with: pip install -r requirements.txt

# Core (Jetson usually has numpy pre-installed but good to list)
numpy
pandas
tqdm
psutil
matplotlib

# Computer Vision & ONNX
opencv-python
onnxruntime-gpu

# Note: FAISS is NOT listed here because on Jetson 
# it's usually built from source or installed via system packages.
# For local testing, you might need: faiss-gpu
